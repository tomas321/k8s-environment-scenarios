---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    service: server-claim2
  name: server-claim2
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: managed-nfs-storage
  resources:
    requests:
      storage: 100Mi
